# A Simple Makefile template for C or C++.
## Sub-commands
- `$ make clean`

## Flags to use (exemple)
- `$ make BUILD_TYPE=Release`
- `$ make COMPILER=/bin/g++`
- `$ make TARGET=ExecutableName TARGET_VERSION=1.2.3`
